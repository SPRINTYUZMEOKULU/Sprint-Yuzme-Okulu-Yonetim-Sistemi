import Link from "next/link"; import { requireProfile } from "@/lib/auth/profile"; import { createClient } from "@/lib/supabase/server"; import "../veli-paneli/veli.css";
export const dynamic="force-dynamic";
export default async function Page(){const p=await requireProfile(["guardian"]);const s=await createClient();const {data:links}=await s.from("guardian_students").select("student_id").eq("guardian_id",p.id);const ids=(links||[]).map((x:any)=>x.student_id);const {data}=ids.length?await s.from("attendance_records").select("id,lesson_date,status,coach_note").in("student_id",ids).order("lesson_date",{ascending:false}):{data:[]};return <main className="guardianShell"><header><div><span>VELİ PANELİ</span><h1>Devam Durumu</h1></div><Link href="/veli-paneli">Geri</Link></header><section className="guardianGrid" style={{gridTemplateColumns:"1fr"}}><article>{(data||[]).map((a:any)=><div className="row" key={a.id}><span>{new Date(a.lesson_date).toLocaleDateString("tr-TR")} {a.coach_note?`— ${a.coach_note}`:""}</span><b>{a.status==="present"?"Katıldı":a.status==="excused"?"Mazeretli":"Katılmadı"}</b></div>)}{!data?.length&&<p className="empty">Henüz yoklama kaydı bulunmuyor.</p>}</article></section></main>}
